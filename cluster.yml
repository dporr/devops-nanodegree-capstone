    apiVersion: eksctl.io/v1alpha5
    kind: ClusterConfig
    metadata:
      name: eks-capstone
      region: us-east-2

     

    nodeGroups:
      - name: ng-1-workers
        instanceType: t2.micro
        desiredCapacity: 2
